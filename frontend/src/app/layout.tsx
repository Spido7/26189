import type { Metadata } from "next";
import { Inter, JetBrains_Mono } from "next/font/google";
import "./globals.css";

const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
  display: "swap",
});

const jetbrainsMono = JetBrains_Mono({
  variable: "--font-jetbrains-mono",
  subsets: ["latin"],
  display: "swap",
});

export const metadata: Metadata = {
  title: "DFIR // INTELLIGENCE COMMAND - Law Enforcement Cyber Forensics Workstation",
  description: "Law-enforcement cyber-intelligence, criminal network knowledge graph, and digital forensics workstation.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="dark">
      <body
        className={`${inter.variable} ${jetbrainsMono.variable} antialiased bg-bg-base text-text-primary h-screen w-screen overflow-hidden flex flex-col`}
      >
        {children}
      </body>
    </html>
  );
}
